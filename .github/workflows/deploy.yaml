name: Deploy

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options:
          - staging
          - production

permissions:
  contents: read

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: github.event.release.prerelease == true || (github.event_name == 'workflow_dispatch' && inputs.environment == 'staging')
    environment:
      name: staging
      url: https://staging.pcomparator.app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: yarn
          
      - name: Install dependencies
        working-directory: ./pcomparator
        run: yarn install --frozen-lockfile
        
      - name: Build application
        working-directory: ./pcomparator
        run: yarn build
        env:
          NODE_ENV: production
          
      - name: Deploy to staging
        run: |
          echo "🚀 Deploying to staging environment..."
          # Ajoute ici tes commandes de déploiement staging
          # Par exemple: rsync, docker push, kubectl apply, etc.

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.event.release.prerelease == false || (github.event_name == 'workflow_dispatch' && inputs.environment == 'production')
    environment:
      name: production
      url: https://pcomparator.app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: yarn
          
      - name: Install dependencies
        working-directory: ./pcomparator
        run: yarn install --frozen-lockfile
        
      - name: Build application
        working-directory: ./pcomparator
        run: yarn build
        env:
          NODE_ENV: production
          
      - name: Deploy to production
        run: |
          echo "🚀 Deploying to production environment..."
          # Ajoute ici tes commandes de déploiement production
          # Par exemple: rsync, docker push, kubectl apply, etc.
